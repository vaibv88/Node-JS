var http = require('require');

var options = {
    host :"www.google.com",
    port:80,
    path:'/index.html'
};

http.get(options , function(res) {
    console.log("Status code is:", +res.statusCode);

});
